<h1 class="page-title">Orden #<%= @order.id %></h1>
<p class="muted">Tienda: <%= @order.store.name %> · Creada: <%= l(@order.created_at, format: :long) %></p>

<% @order.suborders.each do |suborder| %>
  <section class="group">
    <h3>Suborden · Proveedor: <%= suborder.provider.name %></h3>
    <table>
      <thead>
        <tr>
          <th>Producto</th>
          <th>Precio unit. al comprar</th>
          <th>Cantidad</th>
          <th>Subtotal</th>
        </tr>
      </thead>
      <tbody>
        <% suborder.suborder_items.each do |item| %>
          <tr>
            <td><%= item.product.name %></td>
            <td><%= money(item.unit_price_cents) %></td>
            <td><%= item.quantity %></td>
            <td><%= money(item.line_total_cents) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <p class="subtotal text-right">Subtotal suborden: <%= money(suborder.subtotal_cents) %></p>
  </section>
<% end %>

<p class="total text-right">Total de la orden: <%= money(@order.total_cents) %></p>

<p><%= link_to "Volver al catálogo", root_path, class: "btn" %></p>